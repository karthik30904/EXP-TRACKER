# Frontend

This directory contains the Phase 2 Next.js app shell for the expense tracker.

## Run

```bash
cd frontend
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` if the backend is not running on `http://localhost:8001`.

